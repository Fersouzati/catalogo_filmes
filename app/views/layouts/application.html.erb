<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Catalogo Filmes" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.svg" type="image/svg+xml">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    
    <header class="main-header">
      <nav class="main-nav container"> 
        <%= link_to "Catálogo de Filmes", root_path, class: "navbar-brand" %>
        <div class="user-actions">
          <% if user_signed_in? %>
            <span class="user-email">Logado como: <%= current_user.email %></span>
            
            <%= link_to 'Editar perfil', edit_user_registration_path, class: "btn-user-action btn-edit-profile" %>
            <%= button_to 'Sair', destroy_user_session_path, method: :delete, class: "btn-user-action btn-logout", form_class: "d-inline" %> 
          <% else %>
            <%= link_to 'Cadastrar-se', new_user_registration_path, class: "nav-link" %>
            <%= link_to 'Login', new_user_session_path, class: "nav-link" %>
          <% end %>
        </div>
      </nav>
    </header>
    


    
    <main class="container main-content"> 
      
      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>

      <%= yield %> 
    </main>
    


    
    <footer class="main-footer">
      <div class="container"> 
        <p>&copy; <%= Time.current.year %> Catálogo de Filmes. Feito por Nanda!</p>
      </div>
    </footer>
    <%# --- FIM DO RODAPÉ --- %>


    
    <%= link_to "↑ Topo", "#",
                id: "back-to-top",
                data: { controller: "back-to-top", action: "click->back-to-top#scrollToTop" },
                style: "display: none;" %>
  </body>
</html>